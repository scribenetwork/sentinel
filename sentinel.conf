# specify path to scribe.conf or leave blank
# default is the same as ScribeCore
#scribe_conf=/home/scribe/.scribecore/scribe.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
